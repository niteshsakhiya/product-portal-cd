apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.frontend.name }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.issuer }}
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/enable-cors: "false"

spec:
  tls:
  - hosts:
    - {{ .Values.frontend.name }}.{{ .Values.domain }}
    secretName: {{ .Values.frontend.name }}.{{ .Values.domain }}-tls
  rules:
  - host: {{ .Values.frontend.name }}.{{ .Values.domain }}
    http:
      paths:

      - path: {{ .Values.frontend.registration.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.frontend.registration.name }}
            port:
              number: {{ .Values.frontend.portService }}

      - path: {{ .Values.frontend.portal.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.frontend.portal.name }}
            port:
              number: {{ .Values.frontend.portService }}
