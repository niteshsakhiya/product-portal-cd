apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backend.userAdministration.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.backend.portService }}
    targetPort: {{ .Values.backend.portContainer }}
  selector:
    app: {{ .Values.backend.userAdministration.name }}
