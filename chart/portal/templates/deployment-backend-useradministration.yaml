apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backend.userAdministration.name }}
spec:
  replicas: {{ .Values.backend.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.backend.userAdministration.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.backend.userAdministration.name }}
    spec:
      containers:
      - name: {{ .Values.backend.userAdministration.name }}
        image: {{ .Values.backend.userAdministration.image }}
        imagePullPolicy: "Always"
        ports:
        - containerPort: {{ .Values.backend.portContainer }}
        env:
        - name: "ASPNETCORE_ENVIRONMENT"
          value: "Kubernetes"
        volumeMounts:
        - name: secrets
          mountPath: /app/secrets
          readOnly: true
      volumes:
      - name: secrets
        secret:
          secretName: secret-useradministration-appsettings
