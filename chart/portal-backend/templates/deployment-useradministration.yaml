apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.useradministration.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.useradministration.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.useradministration.name }}
    spec:
      containers:
      - name: {{ .Values.useradministration.name }}
        image: {{ .Values.useradministration.image }}
        imagePullPolicy: "Always"
        ports:
        - containerPort: {{ .Values.portContainer }}
        env:
        - name: "ASPNETCORE_ENVIRONMENT"
          value: "Kubernetes"
        volumeMounts:
        - name: secrets
          mountPath: /app/secrets
          readOnly: true
      volumes:
      - name: secrets
        secret:
          secretName: secret-useradministration-appsettings
