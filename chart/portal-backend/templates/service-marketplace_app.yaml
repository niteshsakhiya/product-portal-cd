apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.marketplace_app.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.portService }}
    targetPort: {{ .Values.portContainer }}
  selector:
    app: {{ .Values.marketplace_app.name }}
