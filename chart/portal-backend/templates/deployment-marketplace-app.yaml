apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.marketplace-app.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.marketplace-app.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.marketplace-app.name }}
    spec:
      containers:
      - name: {{ .Values.marketplace-app.name }}
        image: {{ .Values.marketplace-app.image }}
        imagePullPolicy: "Always"
        ports:
        - containerPort: {{ .Values.portContainer }}
        env:
        - name: "ASPNETCORE_ENVIRONMENT"
          value: "Kubernetes"
        volumeMounts:
        - name: secrets
          mountPath: /app/secrets
          readOnly: true
      volumes:
      - name: secrets
        secret:
          secretName: secret-marketplace-app-appsettings
